{
  "name": "wia",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check public/app.js && node --check public/sw.js",
    "test": "node --test tests/*.test.mjs",
    "build": "node tests/build.mjs"
  }
}
